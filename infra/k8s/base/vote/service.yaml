apiVersion: v1
kind: Service
metadata:
  name: vote
  namespace: voting-app
spec:
  selector:
    app: vote
    version: blue  
  ports:
    - port: 5000
      targetPort: 5000
      
  type: NodePort
