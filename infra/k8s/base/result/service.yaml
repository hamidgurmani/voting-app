apiVersion: v1
kind: Service
metadata:
  name: result
  namespace: voting-app
spec:
  selector:
    app: result
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 30008
  type: NodePort
