apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: voting-app-staging
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/hamidgurmani/voting-app.git
    targetRevision: main
    path: infra/k8s/overlays/staging

  destination:
    server: https://kubernetes.default.svc
    namespace: voting-app

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
